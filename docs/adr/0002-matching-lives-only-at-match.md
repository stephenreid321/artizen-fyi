# Fund matching lives only at /match

The fund-alignment panel was dropped from project pages so that matching has one surface (commit 1a99bfb). Ranking, its payload split, its filters and its privacy story are kept correct in one place, and project pages stay simple Stash reads. A project page may link to `/match`; it does not embed a ranking.
